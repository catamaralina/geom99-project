# Create a new folder
mkdir images

# Move into the new folder
cd folder_name

# Optionally, create files in the folder
# For example, create an empty README.md file
touch README.md

# Move back to the root directory of your project
cd ..

# Stage the changes
git add .

# Commit the changes with a descriptive message
git commit -m "Added a new folder named 'images'"

# Push the changes to GitHub
git push origin main
